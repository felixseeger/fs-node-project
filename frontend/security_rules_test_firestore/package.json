{
  "name": "security_rules_test_firestore",
  "version": "1.0.0",
  "description": "Firestore security rules tests",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "emulator:start": "firebase emulators:start --only firestore"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "firebase": "^10.8.0",
    "jest": "^29.7.0"
  }
}
